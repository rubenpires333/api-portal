package com.api_portal.backend.modules.api.domain.enums;

public enum ApiVisibility {
    PUBLIC,         // Visível para todos
    PRIVATE,        // Visível apenas com aprovação
    INTERNAL        // Visível apenas para organização
}
